// Using pointers to the elements in the featured section 
const leftarrow=document.getquerySelector('.left-arrow');
const rightarrow=document.getquerySelector('.right-arrow');
const slider=document.getquerySelector('.carousel__slider');

// Number of the radio button that is currently selected 
var cardIndex=document.querySelector('input[name="slide"]:checked');